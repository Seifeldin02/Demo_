import { FunctionComponent } from "react";
import styles from "./Vector1.module.css";

const Vector1: FunctionComponent = () => {
  return <div className={styles.vectorDiv} />;
};

export default Vector1;
