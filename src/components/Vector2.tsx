import { FunctionComponent } from "react";
import styles from "./Vector2.module.css";

const Vector2: FunctionComponent = () => {
  return <div className={styles.vectorDiv} />;
};

export default Vector2;
