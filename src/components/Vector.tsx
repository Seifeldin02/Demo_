import { FunctionComponent } from "react";
import styles from "./Vector.module.css";

const Vector: FunctionComponent = () => {
  return <div className={styles.vectorDiv} />;
};

export default Vector;
